# This file exists only to satisfy CI validation of the Terraform root module.
#
# The real deployable configuration lives under `terraform/environments/*`.
# GitHub Actions validates both the root and environment directories.

