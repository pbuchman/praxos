# PromptVault Domain Use Cases

Application services for prompt management.

## Planned Use Cases

- `GetPrompt` - Retrieve prompt by ID or alias
- `ListPrompts` - List prompts with filtering
- `CreatePromptVersion` - Create new prompt version

## Guidelines

- Use cases coordinate models and ports
- Return Result types for all operations
