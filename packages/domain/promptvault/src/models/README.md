# PromptVault Domain Models

Domain entities for prompt template management.

## Planned Models

- `Prompt` - Prompt template with metadata
- `PromptVersion` - Versioned prompt content
- `PromptMetadata` - Tags, categories, usage stats

## Guidelines

- Models are pure data structures with validation
- No external dependencies
- Immutable where possible
