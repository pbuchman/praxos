interface:
  display_name: 'Debug Code Task'
  short_description: 'Investigate IntexuraOS code tasks'
  default_prompt: 'Use $debug-code-task only for /#/code-tasks/task_* URLs or direct task_* IDs, then summarize the task document plus log evidence.'
